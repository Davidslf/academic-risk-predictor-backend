# Configuración de Render.com
# https://render.com/docs/web-services

# Este archivo no es estrictamente necesario ya que Render
# detectará automáticamente Python y usará requirements.txt

# Pero puedes personalizarlo si lo necesitas:

# Build Command (ejecutado una vez al desplegar):
# pip install -r requirements.txt

# Start Command (ejecutado para iniciar el servicio):
# python main.py

# También puedes configurar:
# - Variables de entorno
# - Health check path: /health
# - Auto-deploy desde GitHub

